name: lock-stock-web

services:
  lock-stock-web:
    container_name: lock-stock-web
    image: node:lts
    working_dir: /var/www/html/app
    volumes:
      - ../app/:/var/www/html/app
    ports:
      - "5173:8000"
    command: npm run dev

